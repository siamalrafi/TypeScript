"use strict";
/* interface IVechile {
    startEngine: () => void;
    stopEngine: () => void;
    move(): void;
}

class Vechile implements IVechile {
    constructor(
        public name: string,
        public brand: string,
        public model: number,
    ) {
        this.name = name;
        this.brand = brand;
        this.model = model;
    }
    startEngine() {
        console.log("Starting engine");
    }
    stopEngine() {
        console.log("Stopping engine");
    }
    move() {
        console.log("Moving");
    }
    test(): void {
        console.log("Test");
    }
}

const car = new Vechile("Toyota XX", "Toyota", 44)

console.log(car.name); */ 
